{
  "description": "the script is flat: one block per point, no loops and no branches",
  "args": ["run", "--compile", "compiled.sh"],
  "output_flag": false,
  "file_contains": {
    "compiled.sh": ["[0/3] 0% 1", "[1/3] 33% 2", "[2/3] 66% 3"]
  },
  "file_not_contains": {
    "compiled.sh": ["for ", "while ", "if [", "case ", "${yuclid."]
  }
}
