# TODO

- add: adaptive repetition (repeat until CI on the median is within a target)
- add: examples for real benchmark suites or programs (e.g., GAPBS)
- change: `--fold` pads the shorter metrics of a point with `NaN`
